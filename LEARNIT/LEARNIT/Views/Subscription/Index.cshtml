@model IEnumerable<LEARNIT.Models.Subscription>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subscribers.UserEmail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subscribers.UserEmail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubscriptionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubscriptionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubscriptionID })
        </td>
    </tr>
}

</table>
